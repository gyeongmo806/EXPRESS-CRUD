extends ./layout.pug
block content
    .container
        .section
            .article
            .card
                .card-body
                    .card-title
                        h4= data.title
                    .card-text= data.des
                    a(class="card-link" href="#" onclick=`del(${data.id})`) Delete
                    a(class="card-link" href="#" onclick=`location.href='/update/${data.id}'`) Update
    
        
        
    
    
                    
                